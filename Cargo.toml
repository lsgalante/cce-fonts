[package]
name = "clear-typeface-interface"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { version = "0.14", features = ["svg", "image", "tokio"] }
tokio = { version = "1", features = ["full"] }
